# code_review_assistant/.env.example
# Code Review Assistant Configuration
# Copy this file to .env and update with your values

# ============================================================================
# GOOGLE CLOUD CONFIGURATION (REQUIRED)
# ============================================================================

# Your Google Cloud Project ID (required for all deployments)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud region/location
GOOGLE_CLOUD_LOCATION=us-central1

# Vertex AI Agent Engine ID (required for session persistence)
# Get this after first Agent Engine deployment, then add here
AGENT_ENGINE_ID=

# ============================================================================
# MODEL CONFIGURATION
# ============================================================================

# Use Vertex AI (true) or Google AI Studio (false)
GOOGLE_GENAI_USE_VERTEXAI=true

# API Key for Google AI Studio (only if GOOGLE_GENAI_USE_VERTEXAI=false)
GOOGLE_API_KEY=

# Model for code analysis, style checking, and test running
WORKER_MODEL=gemini-2.5-flash

# Model for feedback synthesis (more nuanced reasoning)
CRITIC_MODEL=gemini-2.5-pro

# ============================================================================
# GRADING PARAMETERS
# ============================================================================

# Minimum score to pass (0.0-1.0)
PASSING_SCORE_THRESHOLD=0.8

# Weights for final grade calculation (must sum to 1.0)
STYLE_WEIGHT=0.3
TEST_WEIGHT=0.5
STRUCTURE_WEIGHT=0.2

# ============================================================================
# SAFETY AND LIMITS
# ============================================================================

# Maximum allowed code length in characters
MAX_CODE_LENGTH=10000

# Maximum timeout for test execution in seconds
MAX_TEST_TIMEOUT=5

# Maximum grading attempts per session
MAX_GRADING_ATTEMPTS=3

# Maximum number of style issues to display
MAX_STYLE_ISSUES_SHOWN=10

# ============================================================================
# LOGGING
# ============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable Google Cloud Logging (for production)
ENABLE_CLOUD_LOGGING=false

# Enable debug mode with verbose output
DEBUG_MODE=false

# Enable distributed tracing
ENABLE_TRACING=false

# ============================================================================
# RUNTIME CONFIGURATION (AUTO-SET BY DEPLOYMENT)
# ============================================================================

# Port for web server (Cloud Run sets this automatically)
# PORT=8080

# ============================================================================
# NOTES
# ============================================================================

# 1. For local development:
#    - Run: gcloud auth application-default login
#    - Use either Vertex AI with gcloud auth or AI Studio with API key
#    - Sessions stored locally in SQLite unless AGENT_ENGINE_ID is set
#
# 2. For Cloud Run deployment:
#    - Ensure GOOGLE_CLOUD_PROJECT is set
#    - Set AGENT_ENGINE_ID for persistent sessions
#    - Use service account with appropriate IAM roles
#
# 3. For Agent Engine deployment:
#    - Set AGENT_ENGINE_ID after first deployment
#    - Sessions automatically persist via Agent Engine
#
# 4. Security:
#    - Never commit your actual .env file to version control
#    - Use Secret Manager for production API keys
#    - Rotate API keys regularly
#
# 5. Session Persistence:
#    - With AGENT_ENGINE_ID: Sessions persist across all deployments
#    - Without AGENT_ENGINE_ID: Local SQLite storage only